apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: {{ .Release.Name }}
spec:
  groups:
  - name: {{ .Release.Name }}
    rules:
    - alert: VersionAlert
      expr: version{job="{{ .Release.Name }}"} == 0
